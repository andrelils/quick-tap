<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quicktap.mapper.MerchantMapper">

    <resultMap id="MerchantResultMap" type="com.quicktap.entity.Merchant">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="logo" property="logo"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="wifi_name" property="wifiName"/>
        <result column="wifi_password" property="wifiPassword"/>
        <result column="audit_status" property="auditStatus"/>
        <result column="status" property="status"/>
        <result column="plan_id" property="planId"/>
        <result column="storage_used" property="storageUsed"/>
        <result column="storage_limit" property="storageLimit"/>
        <result column="text_quota_limit" property="textQuotaLimit"/>
        <result column="image_quota_limit" property="imageQuotaLimit"/>
        <result column="video_quota_limit" property="videoQuotaLimit"/>
        <result column="banner_images" property="bannerImages"/>
        <result column="boss_wechat" property="bossWechat"/>
        <result column="business_hours" property="businessHours"/>
        <result column="shop_images" property="shopImages"/>
        <result column="referrer_code" property="referrerCode"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="MerchantResultMap">
        SELECT * FROM merchant ORDER BY created_at DESC
    </select>

    <select id="selectByStatus" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="selectByAuditStatus" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE audit_status = #{auditStatus} ORDER BY created_at DESC
    </select>

    <select id="selectByAuditStatusPage" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE audit_status = #{auditStatus} ORDER BY created_at DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="selectPage" resultMap="MerchantResultMap">
        SELECT * FROM merchant
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR contact_name LIKE CONCAT('%', #{keyword}, '%') OR contact_phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC LIMIT #{offset}, #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO merchant (name, logo, contact_name, contact_phone, address, wifi_name, wifi_password, audit_status, status, plan_id, storage_used, storage_limit, text_quota_limit, image_quota_limit, video_quota_limit, banner_images, boss_wechat, business_hours, shop_images, referrer_code, description, created_at, updated_at)
        VALUES (#{name}, #{logo}, #{contactName}, #{contactPhone}, #{address}, #{wifiName}, #{wifiPassword}, #{auditStatus}, #{status}, #{planId}, #{storageUsed}, #{storageLimit}, #{textQuotaLimit}, #{imageQuotaLimit}, #{videoQuotaLimit}, #{bannerImages}, #{bossWechat}, #{businessHours}, #{shopImages}, #{referrerCode}, #{description}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE merchant SET
        name = #{name},
        logo = #{logo},
        contact_name = #{contactName},
        contact_phone = #{contactPhone},
        address = #{address},
        wifi_name = #{wifiName},
        wifi_password = #{wifiPassword},
        audit_status = #{auditStatus},
        status = #{status},
        plan_id = #{planId},
        storage_used = #{storageUsed},
        storage_limit = #{storageLimit},
        text_quota_limit = #{textQuotaLimit},
        image_quota_limit = #{imageQuotaLimit},
        video_quota_limit = #{videoQuotaLimit},
        banner_images = #{bannerImages},
        boss_wechat = #{bossWechat},
        business_hours = #{businessHours},
        shop_images = #{shopImages},
        referrer_code = #{referrerCode},
        description = #{description},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM merchant WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM merchant
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR contact_name LIKE CONCAT('%', #{keyword}, '%') OR contact_phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM merchant WHERE status = #{status}
    </select>

    <select id="selectByStatusOrderByCreatedAtDesc" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="selectByReferrerCode" resultMap="MerchantResultMap">
        SELECT * FROM merchant WHERE referrer_code = #{referrerCode}
    </select>

    <!-- Statistics Service Methods -->

    <select id="getStatusByMerchantId" resultType="int">
        SELECT status FROM merchant WHERE id = #{merchantId}
    </select>

    <select id="selectTopMerchantsByRevenue" resultType="map">
        SELECT m.id, m.name, COALESCE(SUM(o.amount), 0) as total_revenue, COUNT(o.id) as order_count
        FROM merchant m
        LEFT JOIN order_record o ON m.id = o.merchant_id AND o.status = 'paid'
        GROUP BY m.id, m.name
        ORDER BY total_revenue DESC
        LIMIT #{limit}
    </select>

    <select id="selectTopMerchantsByOrders" resultType="map">
        SELECT m.id, m.name, COUNT(o.id) as order_count, COALESCE(SUM(o.amount), 0) as total_revenue
        FROM merchant m
        LEFT JOIN order_record o ON m.id = o.merchant_id AND o.status = 'paid'
        GROUP BY m.id, m.name
        ORDER BY order_count DESC
        LIMIT #{limit}
    </select>

    <!-- 按 ID 批量查询商户（仅取 id 和 name） -->
    <select id="selectByIds" resultType="com.quicktap.entity.Merchant">
        SELECT id, name FROM merchant WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>
