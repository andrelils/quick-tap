<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quicktap.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.quicktap.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="plan_id" property="planId"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status"/>
        <result column="expire_at" property="expireAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="OrderResultMap">
        SELECT * FROM order_record WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" resultMap="OrderResultMap">
        SELECT * FROM order_record WHERE order_no = #{orderNo}
    </select>

    <select id="selectByMerchantId" resultMap="OrderResultMap">
        SELECT * FROM order_record WHERE merchant_id = #{merchantId} ORDER BY created_at DESC
    </select>

    <select id="selectByMerchantIdAndPage" resultMap="OrderResultMap">
        SELECT * FROM order_record WHERE merchant_id = #{merchantId} ORDER BY created_at DESC LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectByStatus" resultMap="OrderResultMap">
        SELECT * FROM order_record WHERE status = #{status} ORDER BY created_at DESC
    </select>

    <select id="selectPage" resultMap="OrderResultMap">
        SELECT * FROM order_record ORDER BY created_at DESC LIMIT #{offset}, #{pageSize}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_record (order_no, merchant_id, plan_id, amount, status, expire_at, created_at, updated_at)
        VALUES (#{orderNo}, #{merchantId}, #{planId}, #{amount}, #{status}, #{expireAt}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE order_record SET
        order_no = #{orderNo},
        merchant_id = #{merchantId},
        plan_id = #{planId},
        amount = #{amount},
        status = #{status},
        expire_at = #{expireAt},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM order_record WHERE id = #{id}
    </delete>

    <select id="countByMerchantId" resultType="long">
        SELECT COUNT(*) FROM order_record WHERE merchant_id = #{merchantId}
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM order_record WHERE status = #{status}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM order_record
    </select>

    <!-- Statistics Service Methods -->

    <select id="sumTotalAmount" resultType="double">
        SELECT COALESCE(SUM(amount), 0) FROM order_record WHERE status = '1'
    </select>

    <select id="sumAmountByMerchantId" resultType="double">
        SELECT COALESCE(SUM(amount), 0) FROM order_record WHERE merchant_id = #{merchantId} AND status = '1'
    </select>

    <select id="selectOrdersGroupedByDate" resultType="map">
        SELECT DATE(created_at) as date, COUNT(*) as count
        FROM order_record
        WHERE created_at BETWEEN #{startDateTime} AND #{endDateTime}
        GROUP BY DATE(created_at)
        ORDER BY date ASC
    </select>

    <select id="selectRevenueGroupedByDate" resultType="map">
        SELECT DATE(created_at) as date, COALESCE(SUM(amount), 0) as total
        FROM order_record
        WHERE created_at BETWEEN #{startDateTime} AND #{endDateTime} AND status = '1'
        GROUP BY DATE(created_at)
        ORDER BY date ASC
    </select>

</mapper>
