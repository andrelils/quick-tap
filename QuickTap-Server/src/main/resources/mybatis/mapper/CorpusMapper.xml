<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quicktap.mapper.CorpusMapper">

    <!-- 按 ID 查询 -->
    <select id="selectById" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE id = #{id}
    </select>

    <!-- 按内容 ID 查询 -->
    <select id="selectByCorpusId" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE corpus_id = #{corpusId}
    </select>

    <!-- 按商户 ID 查询 -->
    <select id="selectByMerchantId" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE merchant_id = #{merchantId} ORDER BY created_at DESC
    </select>

    <!-- 按分类查询 -->
    <select id="selectByCategory" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE category = #{category} ORDER BY created_at DESC
    </select>

    <!-- 查询所有内容 -->
    <select id="selectAll" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus ORDER BY created_at DESC
    </select>

    <!-- 分页查询 -->
    <select id="selectPage" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus ORDER BY created_at DESC LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 插入内容 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO corpus (corpus_id, merchant_id, title, content, category, tags, status, image_url, view_count, created_by, updated_by, created_at, updated_at)
        VALUES (#{corpusId}, #{merchantId}, #{title}, #{content}, #{category}, #{tags}, #{status}, #{imageUrl}, #{viewCount}, #{createdBy}, #{updatedBy}, NOW(), NOW())
    </insert>

    <!-- 更新内容 -->
    <update id="update">
        UPDATE corpus
        SET title = #{title},
            content = #{content},
            category = #{category},
            tags = #{tags},
            status = #{status},
            image_url = #{imageUrl},
            view_count = #{viewCount},
            updated_by = #{updatedBy},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 按 ID 删除 -->
    <delete id="deleteById">
        DELETE FROM corpus WHERE id = #{id}
    </delete>

    <!-- 按内容 ID 删除 -->
    <delete id="deleteByCorpusId">
        DELETE FROM corpus WHERE corpus_id = #{corpusId}
    </delete>

    <!-- 查询总数 -->
    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM corpus
    </select>

    <!-- 按商户 ID 查询总数 -->
    <select id="countByMerchantId" resultType="long">
        SELECT COUNT(*) FROM corpus WHERE merchant_id = #{merchantId}
    </select>

    <!-- 永久删除回收站中超过指定时间的语料 -->
    <delete id="deletePermanentlyTrashedCorpus">
        DELETE FROM corpus WHERE status = 'trashed' AND updated_at &lt; #{beforeTime}
    </delete>

    <!-- ========== 分页查询（修复内存分页问题） ========== -->

    <!-- 按分类分页查询（仅正常状态 status=1） -->
    <select id="selectByCategoryPage" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE category = #{category} AND status = 1
        ORDER BY created_at DESC LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 按分类统计总数（仅正常状态 status=1） -->
    <select id="countByCategory" resultType="long">
        SELECT COUNT(*) FROM corpus WHERE category = #{category} AND status = 1
    </select>

    <!-- 回收站分页查询（指定商户，status=0 已删除） -->
    <select id="selectTrashByMerchantIdPage" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus WHERE merchant_id = #{merchantId} AND status = 0
        ORDER BY updated_at DESC LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 回收站统计总数（指定商户，status=0） -->
    <select id="countTrashByMerchantId" resultType="long">
        SELECT COUNT(*) FROM corpus WHERE merchant_id = #{merchantId} AND status = 0
    </select>

    <!-- 按关键词模糊查询候选集（仅正常状态，用于 search 先 SQL 过滤再内存评分） -->
    <select id="selectByMerchantIdAndKeyword" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus
        WHERE merchant_id = #{merchantId} AND status = 1
          AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY created_at DESC
    </select>

    <!-- 全局按关键词模糊查询候选集（仅正常状态，用于全局 search 优化） -->
    <select id="selectByKeyword" resultType="com.quicktap.entity.Corpus">
        SELECT * FROM corpus
        WHERE status = 1
          AND (title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY created_at DESC
    </select>

</mapper>
