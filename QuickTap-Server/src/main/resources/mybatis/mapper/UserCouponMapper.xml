<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quicktap.mapper.UserCouponMapper">

    <!-- 按 ID 查询 -->
    <select id="selectById" resultType="com.quicktap.entity.UserCoupon">
        SELECT * FROM user_coupon WHERE id = #{id}
    </select>

    <!-- 按用户 ID 查询 -->
    <select id="selectByUserId" resultType="com.quicktap.entity.UserCoupon">
        SELECT * FROM user_coupon WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <!-- 按用户 ID 和状态查询 -->
    <select id="selectByUserIdAndStatus" resultType="com.quicktap.entity.UserCoupon">
        SELECT * FROM user_coupon WHERE user_id = #{userId} AND status = #{status} ORDER BY created_at DESC
    </select>

    <!-- 按用户 ID 和卡券 ID 查询 -->
    <select id="selectByUserIdAndCouponId" resultType="com.quicktap.entity.UserCoupon">
        SELECT * FROM user_coupon WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </select>

    <!-- 插入用户卡券关系 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_coupon (user_id, coupon_id, merchant_id, status, created_at, updated_at)
        VALUES (#{userId}, #{couponId}, #{merchantId}, #{status}, NOW(), NOW())
    </insert>

    <!-- 更新用户卡券关系 -->
    <update id="update">
        UPDATE user_coupon
        SET status = #{status},
            used_at = #{usedAt},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 按 ID 删除 -->
    <delete id="deleteById">
        DELETE FROM user_coupon WHERE id = #{id}
    </delete>

    <!-- 按用户 ID 和卡券 ID 删除 -->
    <delete id="deleteByUserIdAndCouponId">
        DELETE FROM user_coupon WHERE user_id = #{userId} AND coupon_id = #{couponId}
    </delete>

    <!-- 按用户 ID 和状态查询总数 -->
    <select id="countByUserIdAndStatus" resultType="long">
        SELECT COUNT(*) FROM user_coupon WHERE user_id = #{userId} AND status = #{status}
    </select>

</mapper>
