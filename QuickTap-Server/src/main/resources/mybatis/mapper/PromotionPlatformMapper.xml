<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.quicktap.mapper.PromotionPlatformMapper">

    <resultMap id="PromotionPlatformResultMap" type="com.quicktap.entity.PromotionPlatform">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="icon" property="icon"/>
        <result column="jump_mode" property="jumpMode"/>
        <result column="scheme_template" property="schemeTemplate"/>
        <result column="web_url_template" property="webUrlTemplate"/>
        <result column="required_params" property="requiredParams"/>
        <result column="optional_params" property="optionalParams"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="color" property="color"/>
        <result column="description" property="description"/>
        <result column="miniprogram_appid" property="miniprogramAppid"/>
        <result column="miniprogram_path_template" property="miniprogramPathTemplate"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform WHERE id = #{id}
    </select>

    <select id="selectByCode" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform WHERE code = #{code}
    </select>

    <select id="selectAll" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform ORDER BY sort ASC, created_at DESC
    </select>

    <select id="selectByStatus" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform WHERE status = #{status} ORDER BY sort ASC
    </select>

    <select id="selectPage" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform ORDER BY sort ASC LIMIT #{offset}, #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO promotion_platform (code, name, icon, jump_mode, scheme_template, web_url_template, required_params, optional_params, sort, status, color, description, miniprogram_appid, miniprogram_path_template, created_at, updated_at)
        VALUES (#{code}, #{name}, #{icon}, #{jumpMode}, #{schemeTemplate}, #{webUrlTemplate}, #{requiredParams}, #{optionalParams}, #{sort}, #{status}, #{color}, #{description}, #{miniprogramAppid}, #{miniprogramPathTemplate}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE promotion_platform SET
        code = #{code},
        name = #{name},
        icon = #{icon},
        jump_mode = #{jumpMode},
        scheme_template = #{schemeTemplate},
        web_url_template = #{webUrlTemplate},
        required_params = #{requiredParams},
        optional_params = #{optionalParams},
        sort = #{sort},
        status = #{status},
        color = #{color},
        description = #{description},
        miniprogram_appid = #{miniprogramAppid},
        miniprogram_path_template = #{miniprogramPathTemplate},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM promotion_platform WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM promotion_platform
    </select>

    <!-- 按状态查询，按排序号倒序 -->
    <select id="selectByStatusOrderBySort" resultMap="PromotionPlatformResultMap">
        SELECT * FROM promotion_platform WHERE status = #{status} ORDER BY sort DESC, created_at DESC
    </select>

</mapper>
