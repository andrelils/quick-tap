-- ============================================================
-- QuickTap-Server v1.5.0 升级脚本
-- 功能变更：
--   1. coupon 表新增 link 字段，用于存储第三方平台优惠券跳转链接
--      用户在小程序详情页点击领取优惠券后，将跳转到此 link 对应的第三方平台完成领取
-- ============================================================

-- 1. coupon 表新增 link 字段（第三方平台跳转链接）
ALTER TABLE `coupon`
  ADD COLUMN `link` VARCHAR(512) NULL COMMENT '第三方平台跳转链接，用户点击领取后跳转至此链接' AFTER `status`;

-- 验证字段是否添加成功
-- DESCRIBE `coupon`;
